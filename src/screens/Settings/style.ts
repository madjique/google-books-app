import { StyleSheet } from "react-native";

export const screenStyle = StyleSheet.create({
  titlePageHeaderText : {
    padding : 0,
    margin : -1
  },
  disconnectButton : {
    marginTop : "auto",
    marginBottom : 50
  }
}) 